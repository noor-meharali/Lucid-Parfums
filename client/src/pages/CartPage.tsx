import { PagePlaceholder } from '@/components/common/PagePlaceholder';

export function CartPage() {
  return (
    <PagePlaceholder
      title="Cart"
      description="Cart management will be built in the Cart & Wishlist part."
    />
  );
}
