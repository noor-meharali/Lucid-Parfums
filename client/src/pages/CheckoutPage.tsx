import { PagePlaceholder } from '@/components/common/PagePlaceholder';

export function CheckoutPage() {
  return (
    <PagePlaceholder
      title="Checkout"
      description="The checkout flow will be built in the Checkout & Orders part."
    />
  );
}
